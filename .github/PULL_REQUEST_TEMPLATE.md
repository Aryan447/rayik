# Pull request

## What changed (one line per item)

-

## Verification (check all that apply)

- [ ] `git diff --check` clean
- [ ] Unit tests pass (`:app:testFossMobileUniversalDebugUnitTest`)
- [ ] Release assemble/bundle CI green on this branch
- [ ] Degraded-streaming state + retry covered (or N/A with reason)
- [ ] Rotation + tablet + dark/light pass (or N/A with reason)

## Playback impact

- [ ] No change to resolve/playback paths, or device repro re-run on enforcement-hit mobile data (paste Copy-details first line):
